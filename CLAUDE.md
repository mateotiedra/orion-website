@AGENTS.md

# Development

- Do NOT run `pnpm dev` to test code — it conflicts with the user's local dev server already running.
- Use `pnpm run build:check` instead to verify the code compiles without errors.
